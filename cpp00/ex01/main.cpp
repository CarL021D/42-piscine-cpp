#include <iostream>

int main(int ac, char **av)
{
	std::string input;
	
	while (true)
	{
		std::getline(std::cin, input);
		if (input == "ADD")

		else if (input == "SEARCH")

		else if (input == "EXIT")
			return 0;
	}
}